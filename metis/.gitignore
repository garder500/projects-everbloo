.devenv/*
# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
# pixi environments
.pixi/*
!.pixi/config.toml

# Sub-repositories (nested git repos - managed separately)
api-aerial/
api-dashboard/
front-dashboard/
front-reservation/
